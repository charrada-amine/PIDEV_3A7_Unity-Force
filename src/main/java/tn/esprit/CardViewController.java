/*package tn.esprit;

import javafx.fxml.FXML;
import javafx.scene.control.Label;
import tn.esprit.models.profile;

public class CardViewController {

    @FXML
    private Label sourceEnergetiqueLabel;

    @FXML
    private Label consommationJourLabel;

    @FXML
    private Label consommationMoisLabel;

    @FXML
    private Label coutEstimeLabel;

    @FXML
    private Label dureActiviteLabel;

    // Cette méthode définit les valeurs dans les labels
    public void setProfil(profile profil) {
        sourceEnergetiqueLabel.setText(profil.getSourceEnergetique());
        consommationJourLabel.setText(profil.getConsommationJour());
        consommationMoisLabel.setText(profil.getConsommationMois());
        coutEstimeLabel.setText(profil.getCoutEstime());
        dureActiviteLabel.setText(profil.getDureeActivite());
    }
}*/
